<br/><% if @student.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@student.errors.count, "error") %> prohibited
      this Book from being saved:</h2>
    <ul>
    <% @student.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<h2>Create Student</h2>
<%= form_for :student, url: students_path do |f| %>
  
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.submit "Create Student", :class => "btn btn-primary btn-sm" %>
  </p>
<% end %>
 
<%= link_to 'Back', students_path, :class => "btn btn-info btn-sm" %>