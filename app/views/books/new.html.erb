<h2>New Book</h2>
<%= form_for :book, url: books_path do |f| %>

<% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited
      this Book from being saved:</h2>
    <ul>
    <% @book.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
  
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :author %><br>
    <%= f.text_field :author %>
  </p>

  <p>
    <%= f.label :no_of_copies %><br>
    <%= f.text_field :no_of_copies %>
  </p>

  <p>
    <%= f.check_box :available %>
    <%= f.label :available %>
  </p>
 
  <p>
    <%= f.submit "Create Book", :class => "btn btn-primary btn-sm"  %>
  </p>
<% end %>
 
<%= link_to 'Back', books_path, :class => "btn btn-info btn-sm" %>